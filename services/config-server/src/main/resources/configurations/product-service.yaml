spring:
  datasource:
    url: jdbc:postgresql://ms_pg_sql:5432/product_service
    username: victor
    password: victor
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8020